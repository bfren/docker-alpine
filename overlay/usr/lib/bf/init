#!/bin/sh

set -euo pipefail


#======================================================================================================================
# Run custom initialisation files.
#======================================================================================================================

INIT=/etc/bf/init.d
for FILE in `ls ${INIT} | sort` ; do
    bf-debug "${FILE}: running."
    chmod +x ${INIT}/${FILE} && ${INIT}/${FILE}
    bf-debug "${FILE}: done."
done
